name: Nightly Build

on:
  schedule:
    - cron: '0 3 * * *' # 3AM UTC (00:00 BRT no horário de verão)
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to build'
        required: false
        default: 'main'

jobs:
  build:
    name: Generate Nightly Android Build
    runs-on: ubuntu-latest
    timeout-minutes: 225  # Both queue and build time (3h45m)
    outputs:
      build_id: ${{ steps.build-step.outputs.build_id }}
      build_status: ${{ steps.build-status.outputs.build_status }}
      build_url: ${{ steps.build-status.outputs.build_url }}
      nightly_version: ${{ steps.build-step.outputs.nightly_version }}
      original_version: ${{ steps.build-step.outputs.original_version }}
      timestamp: ${{ steps.build-step.outputs.timestamp }}
      commit_hash: ${{ steps.commit-info.outputs.commit_hash }}
      commit_msg: ${{ steps.commit-info.outputs.commit_msg }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch || 'main' }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Cache dependencies
        uses: actions/cache@v4
        id: cache-dependencies
        with:
          path: |
            node_modules
            ~/.npm
            ~/.cache
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm ci

      - name: Setup Expo tools
        run: npm install -g eas-cli@latest

      - name: Verify EAS Authentication
        run: |
          if ! eas whoami; then
            exit 1
          fi
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Generate nightly build
        id: build-step
        run: |
          echo "🌙 Generating nightly build..."
          TIMESTAMP=$(date +%Y%m%d%H%M%S)
          ORIGINAL_VERSION=$(node -p "require('./package.json').version")
          NIGHTLY_VERSION="${ORIGINAL_VERSION}-nightly.${TIMESTAMP}"

          echo "original_version=${ORIGINAL_VERSION}" >> $GITHUB_OUTPUT
          echo "nightly_version=${NIGHTLY_VERSION}" >> $GITHUB_OUTPUT
          echo "timestamp=${TIMESTAMP}" >> $GITHUB_OUTPUT

          # Backup original app.json
          cp app.json app.json.backup

          # Modify app.json for nightly build
          node -e "
            const fs = require('fs');
            const appJson = JSON.parse(fs.readFileSync('app.json', 'utf8'));
            appJson.expo.version = '${NIGHTLY_VERSION}';
            fs.writeFileSync('app.json', JSON.stringify(appJson, null, 2));
          "

          echo "🚀 Starting build for version: ${NIGHTLY_VERSION}"

          # Capture build ID and URL from JSON output
          BUILD_JSON=$(eas build --platform android --profile preview --non-interactive --no-wait --json)
          if [ -z "$BUILD_JSON" ]; then
            echo "❌ Failed to start build. Please check your configuration."
            exit 1
          fi

          BUILD_ID=$(echo "$BUILD_JSON" | jq -r '.[0].id')
          echo "build_id=${BUILD_ID}" >> $GITHUB_OUTPUT
          echo "BUILD_ID=${BUILD_ID}" >> $GITHUB_ENV
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Wait for build completion
        id: build-status
        run: |
          echo "⏳ Waiting for build to complete..."

          echo "::group::"
          # Wait time constants (in seconds)
          MAX_QUEUE_WAIT=10800  # 3 hours max queue wait time
          MAX_BUILD_TIME=2700   # 45 minutes max build time
          POLL_INTERVAL=60      # Check every minute

          # Initialize counters
          QUEUE_TIME=0
          BUILD_TIME=0

          # First phase: Wait for build to exit the queue
          echo "Phase 1: Waiting for build to exit the queue..."
          while [ $QUEUE_TIME -lt $MAX_QUEUE_WAIT ]; do
            BUILD_INFO=$(eas build:view $BUILD_ID --json)
            if [ -z "$BUILD_INFO" ]; then
              echo "❌ Failed to retrieve build information"
              exit 1
            fi

            BUILD_STATUS=$(echo "$BUILD_INFO" | jq -r '.status')

            # If the build is no longer in queue, proceed to next phase
            if [ "$BUILD_STATUS" != "IN_QUEUE" ]; then
              echo "✅ Build exited queue after $QUEUE_TIME seconds"
              break
            fi
            
            # Show estimated queue time remaining
            QUEUE_POS=$(echo "$BUILD_INFO" | jq -r '.queuePosition // "unknown"')
            WAIT_TIME=$(echo "$BUILD_INFO" | jq -r '.estimatedWaitTimeLeftSeconds // "unknown"')
            echo "Queue position: $QUEUE_POS, Estimated wait: $WAIT_TIME seconds"
            
            sleep $POLL_INTERVAL
            QUEUE_TIME=$((QUEUE_TIME + POLL_INTERVAL))
          done

          if [ $QUEUE_TIME -ge $MAX_QUEUE_WAIT ]; then
            echo "⚠️ Queue wait time exceeded maximum ($MAX_QUEUE_WAIT seconds)"

            # Cancel the build since it's taking too long in queue
            eas build:cancel --id $BUILD_ID --non-interactive
            
            BUILD_STATUS="queue_timeout"
            echo "build_status=${BUILD_STATUS}" >> $GITHUB_OUTPUT
            echo "BUILD_STATUS=${BUILD_STATUS}" >> $GITHUB_ENV
          else
            # Second phase: Wait for build to complete
            echo "Phase 2: Build in progress, waiting for completion..."
            while [ $BUILD_TIME -lt $MAX_BUILD_TIME ]; do
              BUILD_INFO=$(eas build:view $BUILD_ID --json)
              if [ -z "$BUILD_INFO" ]; then
                echo "❌ Failed to retrieve build information"
                exit 1
              fi
              
              BUILD_STATUS=$(echo "$BUILD_INFO" | jq -r '.status')
              case "$BUILD_STATUS" in
                "FINISHED"|"ERRORED"|"CANCELED"|"FAILED")
                  echo "✅ Build process completed with status: $BUILD_STATUS after $BUILD_TIME seconds"
                  break
                  ;;
              esac
              
              sleep $POLL_INTERVAL
              BUILD_TIME=$((BUILD_TIME + POLL_INTERVAL))
            done
            
            if [ $BUILD_TIME -ge $MAX_BUILD_TIME ]; then
              BUILD_STATUS="build_timeout"
            fi
          fi
          echo "::endgroup::"

          # Get final build URL
          FINAL_URL=$(eas build:view $BUILD_ID --json | jq -r '.artifacts.buildUrl // .artifacts.applicationArchiveUrl // "Not available"')

          echo "build_status=${BUILD_STATUS}" >> $GITHUB_OUTPUT
          echo "build_url=${FINAL_URL}" >> $GITHUB_OUTPUT

          echo "BUILD_STATUS=${BUILD_STATUS}" >> $GITHUB_ENV
          echo "BUILD_URL=${FINAL_URL}" >> $GITHUB_ENV
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Restore app.json
        if: always()
        run: |
          if [ -f app.json.backup ]; then
            mv app.json.backup app.json
            echo "✅ app.json restored"
          fi

      - name: Save commit info
        id: commit-info
        if: always()
        run: |
          echo "commit_hash=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          echo "commit_msg=$(git log -1 --pretty=%B | head -1)" >> $GITHUB_OUTPUT

  publish-release:
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    needs: build
    if: ${{ needs.build.result == 'success' && needs.build.outputs.build_status != 'queue_timeout' && needs.build.outputs.build_status != 'build_timeout' }}
    outputs:
      release_url: ${{ steps.create_release.outputs.html_url }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Capture current date and time
        run: echo "CURRENT_DATE=$(date '+%d/%m/%Y %H:%M:%S')" >> $GITHUB_ENV
      
      - name: Generate changelog
        id: generate_changelog
        run: |
          # Find the most recent nightly tag
          LATEST_TAG=$(git tag -l "nightly-*" --sort=-creatordate | head -n 1)
          
          # Define default commit count for changelog
          COMMIT_COUNT=10
          
          if [ -z "$LATEST_TAG" ]; then
            # If no previous nightly tag exists, use the last 10 commits
            echo "CHANGELOG=$(git log -${COMMIT_COUNT} --pretty=format:"- %s [%h]" | sed 's/"/\\"/g')" >> $GITHUB_ENV
            echo "changelog=$(git log -${COMMIT_COUNT} --pretty=format:"- %s [%h]" | sed 's/"/\\"/g')" >> $GITHUB_OUTPUT
          else
            # Check if there are any commits between last tag and HEAD
            COMMIT_COUNT=$(git rev-list --count ${LATEST_TAG}..HEAD)
            if [ "$COMMIT_COUNT" -eq 0 ]; then
              # No new commits since last nightly build
              echo "CHANGELOG=No changes since last nightly build." >> $GITHUB_ENV
              echo "changelog=No changes since last nightly build." >> $GITHUB_OUTPUT
            else
              # Get commits between the last tag and now
              echo "CHANGELOG=$(git log ${LATEST_TAG}..HEAD --pretty=format:"- %s [%h]" | sed 's/"/\\"/g')" >> $GITHUB_ENV
              echo "changelog=$(git log ${LATEST_TAG}..HEAD --pretty=format:"- %s [%h]" | sed 's/"/\\"/g')" >> $GITHUB_OUTPUT
            fi
          fi

      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        with:
          tag_name: nightly-${{ needs.build.outputs.timestamp || 'fallback-timestamp' }}
          release_name: ${{ needs.build.outputs.nightly_version }} [Nightly Build]
          body: |
            ## 🌙 Nightly Build Report
            - **Date:** ${{ env.CURRENT_DATE }}
            - **Build ID:** ${{ needs.build.outputs.build_id || 'Not available' }}
            - **Commit ID:** ${{ needs.build.outputs.commit_hash || 'Not available' }}
            ---
            ### What's new in this build
            ${{ env.CHANGELOG }}

            ### Links
            - **Build URL:** ${{ needs.build.outputs.build_url != 'Not available' && needs.build.outputs.build_url || 'Not available' }}
          draft: false
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_TOKEN }}

      - name: Download APK from EAS
        if: ${{ needs.build.outputs.build_status == 'FINISHED' && needs.build.outputs.build_url != 'Not available' }}
        run: |
          echo "⬇️ Downloading APK from EAS..."
          curl -L "${{ needs.build.outputs.build_url }}" -o build.apk

      - name: Upload Build Artifact to Release
        uses: actions/upload-release-asset@v1
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./build.apk
          asset_name: nightly-build-${{ needs.build.outputs.timestamp }}.apk
          asset_content_type: application/vnd.android.package-archive
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_TOKEN }}

  send-notification:
    name: Send Discord Notification
    runs-on: ubuntu-latest
    needs:
      - build
      - publish-release
    if: always()
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch || 'main' }}

      - name: Send Discord notification
        run: |
          if [ "${{ needs.build.result }}" = "success" ]; then
            ICON="✅"
            RESULT="SUCCESS"
          else
            ICON="❌"
            RESULT="FAILED"
          fi

          BUILD_URL="${{ needs.build.outputs.build_url != 'Not available' && needs.build.outputs.build_url || ' ' }}"
          LOGS_URL="${{ needs.build.result != 'success' && format('{0}/{1}/actions/runs/{2}', github.server_url, github.repository, github.run_id) || '' }}"
          COMMIT_HASH=${{ needs.build.outputs.commit_hash || 'N/A' }}
          RELEASE_URL=${{ needs.publish-release.outputs.release_url || 'N/A' }}

          curl -H "Content-Type: application/json" -X POST -d "{
            \"content\": \"$ICON **Nightly Build $RESULT**\n\n📱 **Projeto:** Point Tils\n📊 **Status:** ${{ needs.build.result }}\n⚙️ **GitHub Release:** $RELEASE_URL\n📝 **Logs:** ${LOGS_URL:-None}\"
          }" "${{ secrets.DISCORD_WEBHOOK }}"
